# {{.Name}}

{{.Description}}

## 开发

```bash
go mod tidy
```

{{if .Frontend}}
本地调试配置页：

```bash
wxbot-plugin dev -config wxbot-plugin.yaml
```

打开 `http://127.0.0.1:3300`。SDK 会启动前后端、注入宿主上下文，并提供 `wxid_dev` mock 实例。

{{end}}
## 打包

```bash
wxbot-plugin package -config wxbot-plugin.yaml
wxbot-plugin verify -file ./dist/{{.ID}}-{{.Version}}.wxplugin
```

## 说明

当前插件是 process runtime。安装后 wechat-desk 会启动 `backend/bin/{{.ID}}-process`，并把 `/api/plugins/{{.ID}}/api/*` 代理到插件进程。
